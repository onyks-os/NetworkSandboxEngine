# NetworkSandboxEngine
A visual, deterministic network sandbox for testing nftables rules. It uses ephemeral Linux network namespaces (netns) and Scapy to validate firewall logic safely, providing real-time visual feedback via a Svelte/FastAPI interface.
